name: cities_repository
description: A Very Good Project created by Very Good CLI.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=2.19.0 <3.0.0"
  flutter: 3.7.0

dependencies:
  cities_service:
    path: '../../packages/cities_service'

  flutter:
    sdk: flutter

  shared_preferences: ^2.0.18
  weather_repository:
    path: '../../packages/weather_repository'

  
  
 

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
  very_good_analysis: ^4.0.0
